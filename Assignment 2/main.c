#include <stdlib.h>
#include <stdio.h>

int main()
{
    int userInput = 2;

    while (userInput > 1)
    {
        printf("Enter 0 or 1 to STOP.\n");
        printf("Enter the NFL score: ");
        scanf("%d", &userInput);

        printf("Possible combinations of scoring plays: ");
        
    }
    

    return 0;
}